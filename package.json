{
  "name": "portifolio_editor",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  },
  "scripts": {
    "postinstall": "chmod +x node_modules/.bin/*",  // Linux/macOS
    "prebuild": "npm install",                     // Garante dependências atualizadas
    "build": "npx vite build",                     // Evita problemas de permissão
    "dev": "npx vite dev",
    "test": "npx vitest"
  },
  "dependencies": {
    // Dependências de produção (ex: React, Vue)
  },
  "devDependencies": {
    "vite": "^5.0.0",                             // Exemplo com Vite
    "vitest": "^1.0.0"                             // Exemplo de teste
  },
  "browserslist": [                                // Opcional: frontend moderno
    ">0.2%",
    "not dead"
  ]
}